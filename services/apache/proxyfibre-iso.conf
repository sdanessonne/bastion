# Bastion — expose les ISO d'installation (stockées sur le disque de données
# /srv/pxe/iso) en HTTP sur le port 2080, pour le boot PXE (Ubuntu casper, etc.).
Alias /iso /srv/pxe/iso
<Directory /srv/pxe/iso>
    Options +FollowSymLinks
    Require all granted
</Directory>
