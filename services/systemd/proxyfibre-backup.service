[Unit]
Description=Bastion — sauvegarde automatique (base + config + médias + AD)
After=mariadb.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/proxyfibre-backup auto run
Nice=10
IOSchedulingClass=idle
