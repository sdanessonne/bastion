# Bastion — serveur de temps NTP pour le réseau LAN.
# Déployé en /etc/chrony/conf.d/proxyfibre.conf
allow 192.168.182.0/24
# Sert l'heure même sans synchro Internet (source locale de secours).
local stratum 10
